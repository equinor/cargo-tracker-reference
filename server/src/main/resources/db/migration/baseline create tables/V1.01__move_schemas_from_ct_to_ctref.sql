SELECT *
INTO ctref.region
FROM ct.region;

SELECT *
INTO ctref.trading_area
FROM ct.trading_area;

SELECT *
INTO ctref.country
FROM ct.country;

SELECT *
INTO ctref.refinery
FROM ct.refinery;

SELECT *
INTO ctref.grade
FROM ct.grade;

SELECT *
INTO ctref.terminal
FROM ct.terminal;

SELECT *
INTO ctref.company
FROM ct.company;

SELECT *
INTO ctref.analysis
FROM ct.analysis;

SELECT *
INTO ctref.company_ocd_mapping
FROM ct.company_ocd_mapping;

SELECT *
INTO ctref.terminal_ocd_mapping
FROM ct.terminal_ocd_mapping;
